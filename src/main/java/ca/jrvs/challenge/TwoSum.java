package ca.jrvs.challenge;

public class TwoSum {

  // Given a list of ints and a target, find the two indices where the numbers at those indices sum
  // to the target.

  // Store entire list in a dictionary (key = index, value = value)
  // Traverse the list
  // For each element, check if target - element = a value in the dict
  // If it does, return the key from the dict and the current index

}
